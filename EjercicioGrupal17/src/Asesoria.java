public interface Asesoria {

    public void analizarUsuario();

}

    /**
     * Segregación de interfaces: La interfaz Asesoria define un único método analizarUsuario()
     * que es implementado por todas las clases relevantes, pero como es la unica interface del proyecto
     * se cumpliría este principio parcialmente
     */
